# Local docker compose setup for data science
This repo contains a simple setup to quickly setup a docker environment for usual datascience tasks.

## Usage
```
git clone 
cd dc-datascience
docker-compose up
```
